// Example 2

// Creating an array
const numbers = [1, 2, 3, 4, 5];

// Destructuring array elements
const [first, second, third] = numbers; // [first, second, , fourth]

// Displaying destructured values
console.log(first);
console.log(second);
console.log(third); 